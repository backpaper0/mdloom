## Agent skills

### Issue tracker

Issue は GitHub Issues で管理します(`gh` CLI を使用)。See `docs/agents/issue-tracker.md`.

### Domain docs

単一コンテキスト構成(ルート直下に `CONTEXT.md` + `docs/adr/`)。See `docs/agents/domain.md`.
